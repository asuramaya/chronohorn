# chronohorn-schema

This crate is the typed export ABI crate for Chronohorn.

Current scope:

- define the typed `opc-export` manifest
- define the typed learned-state index
- define the typed checksum sidecar contract

This crate does not own runtime logic or fleet orchestration.
