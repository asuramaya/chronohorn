[package]
name = "chronohorn-runtime"
version = "0.1.0"
edition = "2024"

[lib]
name = "chronohorn_runtime"
path = "src/lib.rs"

[dependencies]
chronohorn-schema = { path = "../chronohorn-schema" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
